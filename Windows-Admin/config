# --- Identity Section ---
Host *
    User YourID
    IdentityFile ~/.ssh/id_ed25519

# --- The Gateway ---
Host access
    HostName access.computing.clemson.edu

# This now only acts as a pattern matcher for settings, 
# NOT for rewriting the hostname.
Host babbage* newton titan*
    # Leave HostName out of here so Lua can handle it
    ConnectTimeout 5
